# Core test/runtime dependencies. Keep unit tests deterministic; any live-model
# integrations will be optional and gated behind environment flags.
pytest>=8.0
pydantic>=2.7
python-dotenv>=1.0
numpy>=1.26
rank-bm25>=0.2.2
sentence-transformers>=3.0
langchain>=0.3
langgraph>=0.2
